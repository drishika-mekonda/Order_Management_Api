# Order_Management_Api
Using ASP.NET Core Web API + Identity + JWT, creating a secure Order Management API + SQL Server + EF Core, where users can register and login to access order data (Product Name, Quantity, Unit Price, and Total Amount). Each authenticated user can perform CRUD operations on orders.
